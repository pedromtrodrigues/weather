import { ConvertUnitsPipe } from './convert-units-pipe';

describe('ConvertUnitsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertUnitsPipe();
    expect(pipe).toBeTruthy();
  });
});
